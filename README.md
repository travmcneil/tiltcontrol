# Tilt Control

An Android app that turns your phone into a visual pinball tilt-bob simulator. Pick a location, pick a machine, and see a live representation of that machine's tilt bob — calibrated to how *that specific machine* sits and behaves.

## What it does

Real pinball machines use a hanging "tilt bob" — a weight that swings inside a ring, and completes a circuit (triggering a tilt warning) if the machine is nudged too hard. Tilt Control simulates that using your phone's accelerometer:

- Organize machines by **location** (e.g. the arcade or venue you're playing at)
- Each machine gets its own **calibration** — since every machine sits at a slightly different angle/orientation, a raw accelerometer reading isn't meaningful on its own
- A live on-screen dot swings around a ring in real time, mirroring how a real tilt bob would move, and turns red when it crosses that machine's tilt threshold

## Tech stack

- **Kotlin** — primary language
- **Jetpack Compose** — UI toolkit (declarative, similar in spirit to React)
- **Room** — local SQLite database for storing locations, machines, and calibration data
- **Navigation Compose** — screen-to-screen navigation
- **Android Sensor API** (`SensorManager` / `TYPE_ACCELEROMETER`) — live tilt data

## Project structure

```
com.travis.tiltcontrol
├── data/
│   ├── LocationEntity.kt      — Room entity for a location/venue
│   ├── MachineEntity.kt       — Room entity for a machine (includes calibration fields)
│   ├── LocationDao.kt         — database queries for locations
│   ├── MachineDao.kt          — database queries for machines
│   └── TiltDatabase.kt        — Room database setup (singleton)
├── sensor/
│   └── TiltSensorManager.kt   — wraps the accelerometer, exposes live readings as a Flow
├── ui/
│   ├── theme/                 — Compose theming (auto-generated by the project template)
│   ├── TiltBobView.kt         — the Canvas that draws the moving tilt bob
│   ├── LocationListScreen.kt  — list of locations, add/delete
│   ├── MachineListScreen.kt   — list of machines for a location, add/delete
│   └── TiltViewScreen.kt      — the live tilt bob view + calibration button
├── viewmodel/
│   └── TiltViewModel.kt       — connects the database, sensor, and UI together
└── MainActivity.kt            — app entry point, hosts the NavHost
```

## How calibration works

1. Prop the phone against the machine in the position you'll leave it during play
2. Tap **Calibrate** — this reads the current accelerometer values and saves them as that machine's baseline
3. During play, the tilt bob's on-screen position reflects `current reading − baseline`, so the dot starts centered regardless of the machine's resting angle
4. Movement past a threshold distance from center turns the dot red, simulating a real tilt warning

## Screens

| Screen | Purpose |
|---|---|
| Location List | Choose or add a venue/arcade |
| Machine List | Choose or add a machine within that location |
| Tilt View | Live tilt bob visualization + calibration |

## Building & running

1. Open the project in Android Studio
2. Let Gradle sync complete (Room and Navigation dependencies are already configured in `build.gradle.kts`)
3. Connect a physical Android device via USB with **USB Debugging** enabled (accelerometer testing requires a real device — emulators fake sensor data manually)
4. Select your device from the device dropdown in the toolbar and click **Run ▶**

### Installing a built APK manually

```
adb install app/build/outputs/apk/debug/app-debug.apk
```

If `adb` isn't recognized, either use its full path under `<SDK location>/platform-tools/adb.exe`, or add that folder to your system PATH.

## Roadmap / possible next steps

- Average multiple accelerometer samples during calibration for a more stable baseline (rather than a single instantaneous reading)
- Add a pendulum-style damping/inertia effect to the tilt bob visual for a more physically realistic feel
- Edit/rename existing locations and machines (currently add/delete only)
- Package a signed release build for wider installation beyond your own device
- Port to iOS

## Status

Core functionality complete: location/machine management, live accelerometer visualization, and per-machine calibration are all working end-to-end.
